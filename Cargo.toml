[package]
name = "power"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
windows = { version = "0.62.2", features = ["Win32_System_Power"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
